This CGRA test runner supports parallel RTL simulation and VPD generation. To run one test case (e.g., vvadd_8x8):

  ./test_runner.sh vvadd_8x8 $TOP/bsg_manycore/machines/pod_1x1_cgra_xcel_pod

Ideally you need a qsub job script if you want to parallelize the RTL simulation workloads.
